import gym
from gym import error, spaces, utils
from gym.utils import seeding

class ENV2_NAME( gym.Env ):
    metadata = { 'render.modes': ['human'] }

    def __init__( self ):

    def _step( self, action):

    def _reset( self ):

    def _render( self ):
